RunConfig:
  input_product: str()
  product_update_ancillaries: any(map(key=str()), null())
  product_update_params: any(map(key=str()), null())
  product_path_group:
    input_path: str()
    product_path: str()
    scratch_path: str()
