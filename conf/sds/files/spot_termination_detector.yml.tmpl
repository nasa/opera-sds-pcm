# This is a configuration file that can be passed as an argument to the spot_termination_detector script

# The Mozart REST API, e.g. http://192.168.0.1:8888/api/v0.1
mozart_rest_url: http://{{ MOZART_PVT_IP }}:8888/api/v0.1

# Check for marked for termination every N seconds. Default is 60.
check: 60
